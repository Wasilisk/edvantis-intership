import {StyledTextType} from "../../models/common";

export const mockStyledText: StyledTextType = {
    before_highlight: "Text before",
    highlight: "Highlight text",
    after_highlight: "Text after"
}